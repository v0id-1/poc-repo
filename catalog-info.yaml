apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: techdocs-extra-templates-poc
  description: Proof of concept for a reported TechDocs vulnerability. Harmless.
  annotations:
    # Build the docs from this same repository.
    backstage.io/techdocs-ref: dir:.
spec:
  type: service
  lifecycle: experimental
  owner: guests
